apiVersion: v2
name: generic-backend
description: Golden Path バックエンドアプリ用 wrapper chart（common-app / common-db ライブラリを利用）
type: application
version: 0.1.0
dependencies:
  - name: common-app
    version: "0.3.0"
    repository: "file://../common-app"
  - name: common-db
    version: "0.1.0"
    repository: "file://../common-db"
