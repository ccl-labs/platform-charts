apiVersion: v2
name: generic-frontend
description: Golden Path フロントエンドアプリ用 wrapper chart（common-app ライブラリを利用）
type: application
version: 0.1.0
dependencies:
  - name: common-app
    version: "0.3.0"
    repository: "file://../common-app"
